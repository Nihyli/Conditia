# --- Core API ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17
pydantic==2.10.3
pydantic-settings==2.6.1

# --- Database (async) ---
# SQLite for local dev (default). For Supabase/Postgres, set DATABASE_URL to a
# postgresql+asyncpg URL and add asyncpg below.
SQLAlchemy==2.0.36
aiosqlite==0.20.0
greenlet>=3.0.0
PyJWT==2.10.1
bcrypt>=4.0.0
# asyncpg==0.30.0          # uncomment for Postgres / Supabase

# --- Optional integrations (uncomment when configured) ---
# supabase==2.10.0          # Supabase Storage + Postgres in production
# google-cloud-vision==3.8.0  # real damage detection (otherwise stubbed)
# opencv-python-headless==4.10.0.84  # video frame extraction
