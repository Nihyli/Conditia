# --- Core API ---
fastapi==0.138.0
starlette==1.3.1
# Plain uvicorn (no [standard]) — httptools uses a native .pyd that often fails
# to install on Windows (WinError 5 file lock). watchfiles keeps --reload fast.
uvicorn==0.41.0
watchfiles==1.2.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2
anyio==4.12.1
pillow==12.2.0
supabase==2.31.0

# --- Database (async) ---
# SQLite for local dev (default). For Supabase/Postgres, set DATABASE_URL to a
# Postgres URL — the app rewrites it to the asyncpg driver automatically.
SQLAlchemy==2.0.36
alembic==1.18.4
aiosqlite==0.20.0
asyncpg==0.31.0
greenlet==3.5.2

# --- Optional integrations (uncomment when configured) ---
# supabase==2.10.0          # Supabase Storage + Postgres in production
# google-cloud-vision==3.8.0  # experimental triage; always requires review
# opencv-python-headless==4.10.0.84  # video frame extraction
